import java.util.Scanner;

public class Main1 {
    public static void main(String[] args) 
    {
        int a = 1;
        int b = 2;
        double c = 3.0;
        char d = 'a';
        boolean e = true;
        System.out.println(a);
        System.out.println(b);
        System.out.println(c);
        System.out.println(d);
        System.out.println(d);
        
        System.out.println(a+b);
        System.out.println(a-b);
        System.out.println(a*b);
        System.out.println(b/a);
        System.out.println(b%a);
        
        Scanner keyboard = new Scanner(System.in);
        System.out.println("Enter name");
        String name = keyboard.nextLine();
        System.out.println("Enter age");
        String age = keyboard.nextLine();
        System.out.println("Hello " + name + ", you will be " + age + " next year.");
        
        System.out.println("Enter two ints");
        int first = keyboard.nextInt();
        int second = keyboard.nextInt();
        if(first > second)
        {
            System.out.println("The larger number is: " + first);
        }
        else
        {
            System.out.println("The larger number is: " + second);
        }
        
        System.out.println("Enter an int");
        int input = keyboard.nextInt();
        if(input >= 1)
        {
            System.out.println("Positive");
        }
        else if (input <= -1)
        {
            System.out.println("Negative");
        }
        else
        {
            System.out.println("0");
        }
        
        System.out.println("Enter a number 1-7");
        int day = keyboard.nextInt();
        switch (day){
            case 1:
                System.out.println("Monday");
                break;
            case 2:
                System.out.println("Tuesday");
                break;
            case 3:
                System.out.println("Wednesday");
                break;
            case 4:
                System.out.println("Thursday");
                break;
            case 5:
                System.out.println("Friday");
                break;
            case 6:
                System.out.println("Saturday");
                break;
            case 7:
                System.out.println("Sunday");
                break;
        }
        
        for (int i = 1;  i<=20; i++)
        {
            System.out.println(i);
        }
        
        int inputX = 0;
        int total = 0;
        do {
            System.out.println("Enter an int");
            inputX = keyboard.nextInt();
            total += inputX;
            
        } while (inputX != -1);
        System.out.println(total+1);
        
        for (int i = 1; i <= 5; i++)
        {
            for (int k = 1; k <= i; k++)
            {
                System.out.print("*");
            }
            System.out.println();
        }
        
        System.out.println("Enter five ints");
        int[] anArray = {keyboard.nextInt(), keyboard.nextInt(), keyboard.nextInt(), keyboard.nextInt(), keyboard.nextInt()};
        System.out.println(anArray[0]);
        System.out.println(anArray[1]);
        System.out.println(anArray[2]);
        System.out.println(anArray[3]);
        System.out.println(anArray[4]);
        int sum = anArray[0] + anArray[1] +anArray[2] +anArray[3] +anArray[4];
        int avg = sum/5;
        System.out.println("sum: "+ sum + "avg: " + avg);
        
        int max = anArray[0];
        int min = anArray[0];
        for (int i = 1; i <=5; i++)
        {
            if(anArray[i] > max)
            {
                max = anArray[i];
            }
            if(anArray[i] < min)
            {
                min = anArray[i];
            }
        }
        System.out.println("max: "+ max + "min: " + min);
        
        System.out.println("How many students in your class");
        int numStudents = keyboard.nextInt();
        int[] scores = new int[numStudents];
        int scoreSum = 0;
        int highest = 0;
        int passed = 0;
        for (int i = 0; i < numStudents; i++)
        {
            System.out.println("Enter score for student " + (i + 1));
            scores[i] = keyboard.nextInt();
            
            scoreSum += scores[i];
            
            if (scores[i] > highest)
            {
                highest = scores[i];
            }
            
            if (scores[i] >= 60)
            {
                passed++;
            }
        }
        double averageScore = (double) scoreSum / numStudents;
        System.out.println("Average score: " + averageScore);
        System.out.println("Highest score: " + highest);
        System.out.println("Students passed: " + passed);
                
    }
}
